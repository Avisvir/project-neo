# 📟 PROJECT_NEO: Secure Password Generator

A Matrix-themed, privacy-first password generator. 

### ⚡ Features
- **Zero-Trace:** Everything stays in RAM; nothing is saved to disk.
- **Auto-Destruct:** Sessions wipe after 5 minutes of inactivity.
- **Matrix UI:** Interactive falling code background.

### 🚀 How to Run Locally
1. Install Streamlit: `pip install streamlit`
2. Run the app: `python -m streamlit run streamlit_app.py`